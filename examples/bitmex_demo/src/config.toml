[strategy]
spread = 15
currency_pair = { base = "xbt", quote = "usd" }
max_amount = 200
exchange_account_id = "Bitmex_0"

[[core.exchanges]]
exchange_account_id = "Bitmex_0"
is_margin_trading = true
request_trades = false
subscribe_to_market_data = true
currency_pairs = [{ base = "xbt", quote = "usd" }]
# this has to be there but it is hardcoded in the code
websocket_channels = []

[core.database]
migrations = ["../../binance_demo_new/migrations"]
